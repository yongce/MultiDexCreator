apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

jar {
    manifest {
        attributes("Main-Class": "me.ycdev.android.lib.multidexcreator.MutilDexCreator")
    }
}
